services:
  budget:
    build: .
    ports:
      - "8000:8000"
    volumes:
      - budget-data:/data
    environment:
      - BUDGET_DATA_DIR=/data
    restart: unless-stopped

volumes:
  budget-data:
